/** Monthly video-processing limit for FREE-plan users. PRO/AGENCY are unlimited. */
export const FREE_PLAN_VIDEO_LIMIT = 3;

/** Error code returned to the frontend so it can show the upgrade modal. */
export const PLAN_LIMIT_REACHED_CODE = 'PLAN_LIMIT_REACHED';
